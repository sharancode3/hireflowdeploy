{
  "name": "hire-hub",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "build": "npm -w backend run build && npm -w frontend run build",
    "lint": "npm -w frontend run lint",
    "typecheck": "npm -w backend run typecheck && npm -w frontend run typecheck",
    "check": "npm run typecheck && npm run lint && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
